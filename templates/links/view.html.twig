{% extends 'base.html.twig' %}

{% block body %}
    
    <h3>
        {{ link.name }}
    </h3>
    <p>
        {{ link.description }}
    </p>
    <a href="{{ link.cible }}" target="_blank">
        {{ link.cible }}
    </a>
    <p>
    <a href="{{ path('edit', {id : link.id}) }}">
        Editer
    </a>
    &nbsp;&nbsp;&nbsp;
    <a href="{{ path('home') }}">
        Accueil
    </a>
    </p>
    
{% endblock %}
