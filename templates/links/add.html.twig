{% extends 'base.html.twig' %}

{% block body %}
    <form>
        <div class="form-group">
            {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_rest(form) }}
            <button type="submit" class="btn btn-primary">Valider</button>
            {{ form_end(form) }}
        </div>
        
    </form>
    
    <p>
        <a href="{{ path("home") }}">
            Accueil
        </a>
    </p>
    
{% endblock %}